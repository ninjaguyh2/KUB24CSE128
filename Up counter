entity upCounter is 
 Port ( clk : in STD_LOGIC; 
 q : buffer STD_LOGIC_VECTOR(2 downto 0):=”000”); end upCounter; 
 architecture Behavioral of upCounter is 
begin 
 process(clk) begin 
if(clk'event and clk='1')then
q <= q+1; 
end if; end process; 
end Behavioral;
